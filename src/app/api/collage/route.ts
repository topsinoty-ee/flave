// import { createCollage } from "@/util";

// export async function POST(req: Request) {
//   try {
//     const { images, options } = await req.json();

//     const collage = await createCollage(images, options);
//     return Response.json({ dataUrl: collage });
//   } catch (err) {
//     console.error(err);
//     return new Response("Failed to create collage", { status: 500 });
//   }
// }

export async function POST() {}
